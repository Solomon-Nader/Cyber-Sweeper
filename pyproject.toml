[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cybersweeper"
dynamic = ["version"]
description = "Cyber Sweeper - a Python network inspector: host discovery, port scanning, service identification and vulnerability lookup with CLI and GUI."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Cyber Sweeper Project" }]
keywords = ["network", "scanner", "security", "nmap", "cve", "port-scan"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Topic :: System :: Networking :: Monitoring",
    "Operating System :: OS Independent",
]
dependencies = [
    "python-nmap>=0.7.1",
    "requests>=2.31",
    "reportlab>=4.0",
    "python-docx>=1.1",
]

[project.optional-dependencies]
dev = ["pytest>=7.4", "pytest-cov>=4.1", "ruff>=0.4"]

[project.scripts]
cybersweeper = "cybersweeper.cli:main"

[tool.setuptools.packages.find]
include = ["cybersweeper*"]

[tool.setuptools.package-data]
cybersweeper = ["assets/*"]

[tool.setuptools.dynamic]
version = { attr = "cybersweeper.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501", "UP045"]
